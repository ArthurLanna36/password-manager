import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  emptyStateContainer: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
    paddingVertical: 50,
  },
  emptyText: {
    textAlign: "center",
    fontSize: 18,
    opacity: 0.8,
  },
  emptySubText: {
    textAlign: "center",
    fontSize: 15,
    opacity: 0.6,
    marginTop: 8,
  },
});
